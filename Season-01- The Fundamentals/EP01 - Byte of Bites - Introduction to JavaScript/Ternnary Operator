// 'condition ? expr1 : expr2    ternary operator (evaluates conditon and returm one of two expr ) shorthand of if-else
let age=18;
let result = age >= 18 ? "You are eligible to vote" : "You are not eligible to vote"; // if age >= 18 then result = "You are eligible to vote" else result = "You are not eligible to vote"


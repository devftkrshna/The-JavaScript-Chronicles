// '&&' And Operator; true if and only if both the booleans(operands) are true
let andResult = (true&&false); //andResult is false
let andResult_= (true&&true); //andResult_ is true

// '||' Or Operator; False if and only is both of the booleans(operands) are false
let orResult =(true||false); //orResult is true
let orResult_ =(false||false); //orResult is false

// '!' Not Operator, changee the boolean
let notResult = !true; //false
let notResult_ =!false; //true

console.log(andResult);